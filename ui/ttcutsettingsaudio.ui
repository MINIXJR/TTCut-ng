<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>TTCutSettingsAudio</class>
 <widget class="QGroupBox" name="TTCutSettingsAudio">
  <property name="geometry">
   <rect><x>0</x><y>0</y><width>500</width><height>250</height></rect>
  </property>
  <property name="title"><string>Audio &amp;&amp; Language</string></property>
  <layout class="QVBoxLayout">
   <item>
    <widget class="QGroupBox" name="gbAudioLang">
     <property name="title"><string>Language Preference</string></property>
     <layout class="QFormLayout">
      <item row="0" column="0">
       <widget class="QLabel" name="laLangPref">
        <property name="text"><string>Language order (e.g. deu,eng):</string></property>
        <property name="toolTip"><string>Comma-separated list of preferred audio languages (ISO 639-2 codes, e.g. deu,eng,fra). Controls the order of the audio tracks in the cut dialog.</string></property>
       </widget>
      </item>
      <item row="0" column="1">
       <widget class="QLineEdit" name="leAudioLangPref">
        <property name="placeholderText"><string>e.g. deu,eng,fra</string></property>
        <property name="toolTip"><string>Comma-separated list of preferred audio languages (ISO 639-2 codes, e.g. deu,eng,fra). Controls the order of the audio tracks in the cut dialog.</string></property>
       </widget>
      </item>
     </layout>
    </widget>
   </item>
   <item>
    <widget class="QGroupBox" name="gbAudioOptions">
     <property name="title"><string>Audio Options</string></property>
     <layout class="QVBoxLayout">
      <item>
       <widget class="QCheckBox" name="cbNormalizeAcmod">
        <property name="text"><string>Normalise AC3 channel format at cut points</string></property>
        <property name="toolTip"><string>Normalise the AC3 channel format (acmod) at cut points. Prevents audio artefacts when the broadcaster changes within the stream (e.g. one segment is 5.1 and the next is 2.0).</string></property>
       </widget>
      </item>
      <item>
       <layout class="QFormLayout">
        <item row="0" column="0">
         <widget class="QLabel" name="laBurst">
          <property name="text"><string>Minimum burst jump above surroundings (dB, 0=off):</string></property>
          <property name="toolTip"><string>A burst is reported when its level exceeds the surrounding audio by at least this many dB. Higher = less sensitive; 0 disables burst detection.</string></property>
         </widget>
        </item>
        <item row="0" column="1">
         <widget class="QSpinBox" name="sbBurstMinDelta">
          <property name="minimum"><number>0</number></property>
          <property name="maximum"><number>60</number></property>
          <property name="suffix"><string> dB</string></property>
          <property name="toolTip"><string>A burst is reported when its level exceeds the surrounding audio by at least this many dB. Higher = less sensitive; 0 disables burst detection.</string></property>
         </widget>
        </item>
        <item row="1" column="0">
         <widget class="QLabel" name="laPlaybackChannels">
          <property name="text"><string>Channel layout during playback:</string></property>
          <property name="toolTip"><string>Playback only - the cut is not affected. mpv follows the audio stream by default, so an AC3 track that switches its channel mode within the recording (5.1 &lt;-&gt; 2.0, common in DVB) makes it close and reopen the audio output at every switch, which can be audible as a click. "5.1" pins the output for AC3 tracks so no switch happens; stereo material then simply occupies the front channels. Tracks that are stereo by definition (MP2) are never pinned.</string></property>
         </widget>
        </item>
        <item row="1" column="1">
         <widget class="QComboBox" name="cbPlaybackChannels">
          <property name="toolTip"><string>Playback only - the cut is not affected. mpv follows the audio stream by default, so an AC3 track that switches its channel mode within the recording (5.1 &lt;-&gt; 2.0, common in DVB) makes it close and reopen the audio output at every switch, which can be audible as a click. "5.1" pins the output for AC3 tracks so no switch happens; stereo material then simply occupies the front channels. Tracks that are stereo by definition (MP2) are never pinned.</string></property>
          <item><property name="text"><string>Original (follow the stream)</string></property></item>
          <item><property name="text"><string>5.1 (pin, avoids the switch)</string></property></item>
         </widget>
        </item>
       </layout>
      </item>
     </layout>
    </widget>
   </item>
   <item>
    <spacer><property name="orientation"><enum>Qt::Vertical</enum></property><property name="sizeHint" stdset="0"><size><width>20</width><height>40</height></size></property></spacer>
   </item>
   <item>
    <layout class="QHBoxLayout">
     <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property><property name="sizeHint" stdset="0"><size><width>40</width><height>20</height></size></property></spacer></item>
     <item>
      <widget class="QPushButton" name="btnResetDefaults">
       <property name="text"><string>Reset to defaults</string></property>
       <property name="toolTip"><string>Reset audio burst threshold, AC3 normalisation and the playback channel layout to their factory defaults. The language preference is left unchanged. Changes are applied only when you click OK.</string></property>
      </widget>
     </item>
    </layout>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
